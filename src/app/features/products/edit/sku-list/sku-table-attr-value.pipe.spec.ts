import { SkuTableAttrValuePipe } from './sku-table-attr-value.pipe';

describe('SkuTableAttrValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SkuTableAttrValuePipe();
    expect(pipe).toBeTruthy();
  });
});
